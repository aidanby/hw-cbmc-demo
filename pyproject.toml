[project]
name = "environment"
version = "0.1.0"
description = "RL training environment scaffold."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["uv_build>=0.6.5"]
build-backend = "uv_build"
